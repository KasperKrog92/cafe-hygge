{
  "version": "0.0.1",
  "configurations": [
    {
      "name": "cafe",
      "runtimeExecutable": "python",
      "runtimeArgs": [
        "-c",
        "import http.server, os; http.server.test(HandlerClass=http.server.SimpleHTTPRequestHandler, port=int(os.environ.get('PORT', '8137')))"
      ],
      "port": 8137,
      "autoPort": true
    }
  ]
}
